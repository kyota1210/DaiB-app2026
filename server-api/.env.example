# DB
DB_HOST=localhost
DB_USER=root
DB_PASSWORD='Daily<Bookmark*1210K-dev'
DB_DATABASE=daib_db
PORT=3000

# Supabase Dashboard → Project Settings → API → JWT Secret（anon キーではない）
SUPABASE_JWT_SECRET=

# レガシー自前 JWT（廃止予定・未使用なら空でも可）
JWT_SECRET=
